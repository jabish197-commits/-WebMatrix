import { Router } from "express"; import {listPages,getPage,savePage} from "../controllers/pageController.js"; import authenticate from "../middleware/authenticate.js"; import allowRoles from "../middleware/allowRoles.js"; const router=Router(); router.get("/",listPages); router.get("/:slug",getPage); router.patch("/:id",authenticate,allowRoles("super_admin"),savePage); export default router;
