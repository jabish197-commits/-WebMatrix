import {supabase} from "../config/supabase.js"; export const listUsers=async(req,res)=>{let q=supabase.from("users").select("id,name,email,role,permissions,status,created_at");if(req.query.role)q=q.eq("role",req.query.role);const{data,error}=await q;if(error)throw error;res.json(data);}; export const getUser=async(req,res)=>{const{data,error}=await supabase.from("users").select("id,name,email,role,permissions,status,created_at").eq("id",req.params.id).single();if(error)throw error;res.json(data);};
